{
  "title": "The Invoice Queue Retry",
  "service": "invoice-queue",
  "severity": "SEV-2",
  "alert": "Duplicate invoice work items are appearing after a retry.",
  "timeLimitSeconds": 900,
  "briefing": {
    "objective": "Trace why a retry can enqueue the same invoice work item more than once.",
    "successCriterion": "The Python unittest suite must show that each invoice appears only once after retries.",
    "rootCause": "The queue records every retry without checking whether that invoice is already pending.",
    "evidence": "The verification suite counts the queued invoice IDs after repeated enqueue attempts."
  },
  "serviceDirectory": "invoice-queue",
  "activeFile": "src/invoice_queue.py",
  "execution": {
    "language": "python",
    "runner": "pyodide",
    "testCommand": ["unittest", "discover", "tests"]
  }
}
